@page
@model POS.Pages.api.orders.RecentModel
@{
    // Disable the layout to prevent any HTML from being included in the response
    Layout = null;
    
    // Set the content type to JSON
    Response.ContentType = "application/json";
} 